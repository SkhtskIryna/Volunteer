database {
    url = "jdbc:mysql://volunteer-mysql:3306/volunteer?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    driver = "com.mysql.cj.jdbc.Driver"
    user = "root"
    password = "MyNewPass123!"
}

ktor {
    deployment {
        log = true
        loglevel = INFO
    }
}